# 💡 Проектная работа «Веб-ларек»

## 🚀 Используемый стек

- **HTML** — разметка страниц
- **SCSS** — стилизация интерфейса
- **TypeScript** — типизация и логика приложения
- **Webpack** — сборка проекта

---

## ⚙️ Инструкция по запуску

1. Установите зависимости:
   ```sh
   npm install
   # или
   yarn
   ```

2. Запустите проект в режиме разработки:
   ```sh
   npm run start
   # или
   yarn start
   ```

3. Соберите проект для продакшена:
   ```sh
   npm run build
   # или
   yarn build
   ```

---

## 🏗️ Архитектура проекта

В проекте используется паттерн **MVP (Model-View-Presenter)**:

- **Model (Модель)**  
  Описывает структуру и состояние данных приложения.  
  *Классы и интерфейсы:*  
  - `ProductModel`  
  - `OrderModel`  
  - Типы из [`src/types/index.ts`](src/types/index.ts)

- **View (Представление)**  
  Отвечает за отображение данных и взаимодействие с пользователем.  
  *Компоненты:*  
  - Карточки товаров (`Card`)  
  - Модальные окна (`Modal`)  
  - Корзина (`Basket`)  
  - Формы (`Form`)  
  - SCSS-стили и шаблоны из [`src/common.blocks/`](src/common.blocks/)

- **Presenter (Презентер)**  
  Связывает модель и представление, обрабатывает бизнес-логику, реагирует на действия пользователя и обновляет View.  
  *Классы и утилиты:*  
  - [`EventEmitter`](src/components/base/events.ts) — брокер событий  
  - [`Api`](src/components/base/api.ts) — клиент API  
  - Вспомогательные функции из [`src/utils/utils.ts`](src/utils/utils.ts)

**Связи между слоями:**
- View отправляет события презентеру через брокер событий.
- Presenter получает данные из модели и API, обновляет View.
- Model хранит и предоставляет данные презентеру.

---

### 🧩 Описание базовых классов

- **EventEmitter**  
  [`EventEmitter`](src/components/base/events.ts) — класс-брокер событий, реализует паттерн "наблюдатель".  
  Позволяет подписываться на события, снимать подписку, инициировать события и слушать все события.  
  Взаимодействует с другими классами через публикацию и обработку событий.  
  Не наследует другие классы, используется как отдельний сервис.  
  **Основные методы:**
  - `on<T>(event: string | RegExp, callback: (data: T) => void): void` — подписка на событие.
  - `off(event: string | RegExp, callback: Function): void` — отписка от события.
  - `emit<T>(event: string, data?: T): void` — инициировать событие.
  - `onAll(callback: (event: { eventName: string, data: unknown }) => void): void` — слушать все события.
  - `trigger<T>(event: string, context?: Partial<T>): (data: T) => void` — создать коллбек-триггер.

- **Api**  
  [`Api`](src/components/base/api.ts) — класс для работы с сервером, реализует методы для получения и отправки данных.  
  Используется презентером для получения данных моделей и отправки заказов.  
  Не наследует другие классы, инкапсулирует логику HTTP-запросов.  
  **Основные свойства:**
  - `baseUrl: string` — базовый URL API.
  - `options: RequestInit` — настройки запроса.
  **Основные методы:**
  - `get<T>(uri: string): Promise<T>` — получить данные.
  - `post<T>(uri: string, data: object, method?: 'POST' | 'PUT' | 'DELETE'): Promise<T>` — отправить данные.
  - `handleResponse(response: Response): Promise<object>` — обработка ответа сервера.

- **Card**  
  Класс-компонент для отображения карточки товара.  
  Универсальный: поддерживает разные виды карточек (каталог, корзина, предпросмотр) через параметры конструктора и шаблоны.  
  Взаимодействует с моделью товара и презентером через события и props.  
  Может наследовать базовый класс отображения (например, `BaseView`).  
  **Основные свойства:**
  - `props: ProductViewProps` — свойства для отображения.
  - `element: HTMLElement` — DOM-элемент карточки.
  **Основные методы:**
  - `render(): HTMLElement` — отрисовать карточку.
  - `update(props: ProductViewProps): void` — обновить данные карточки.

- **Basket**  
  Класс-компонент для отображения корзины пользователя.  
  Взаимодействует с моделью заказа, презентером и компонентом Card.  
  Может наследовать базовый класс отображения.  
  **Основные свойства:**
  - `items: ProductModel[]` — список товаров.
  - `total: number` — итоговая сумма.
  **Основные методы:**
  - `render(): HTMLElement` — отрисовать корзину.
  - `update(items: ProductModel[]): void` — обновить список товаров.

- **Form**  
  Класс-компонент для отображения и управления формами заказа и контактов.  
  Взаимодействует с моделью заказа и презентером через события.  
  Может наследовать базовый класс отображения.  
  **Основные свойства:**
  - `fields: object` — значения полей формы.
  - `element: HTMLElement` — DOM-элемент формы.
  **Основные методы:**
  - `render(): HTMLElement` — отрисовать форму.
  - `validate(): boolean` — валидация данных.
  - `submit(): void` — отправка формы.

- **Modal**  
  Класс-компонент для отображения модальных окон (корзина, заказ, успех).  
  Взаимодействует с другими компонентами через props и события.  
  Может наследовать базовый класс отображения.  
  **Основные свойства:**
  - `content: HTMLElement` — содержимое модального окна.
  - `element: HTMLElement` — DOM-элемент модального окна.
  **Основные методы:**
  - `open(): void` — открыть модальное окно.
  - `close(): void` — закрыть модальное окно.
  - `setContent(content: HTMLElement): void` — установить содержимое.

---

### 🖼️ Классы представления (View)

В интерфейсе используются отдельные классы для каждого визуального компонента:

- **Card**  
  Универсальный класс для отображения карточек товаров.  
  Используется для каталога, корзины и предпросмотра.  
  Внешний вид и содержимое карточки определяются props и шаблоном.

- **Basket**  
  Класс для отображения корзины пользователя.  
  Содержит список товаров, итоговую сумму и кнопки управления.

- **Modal**  
  Класс для отображения модальных окон.  
  Используется для корзины, заказа, успеха и других всплывающих окон.

- **OrderForm**  
  Класс для формы оформления заказа (выбор способа оплаты, ввод адреса).  
  Включает поля и кнопки для перехода к следующему этапу.

- **ContactsForm**  
  Класс для формы ввода контактных данных (email, телефон).  
  Реализует валидацию и отправку данных.

- **OrderSuccess**  
  Класс для окна успешного оформления заказа.  
  Показывает итоговую сумму и кнопку возврата к покупкам.

Каждый класс реализует методы:
- `render(): HTMLElement` — отрисовка компонента.
- `update(props): void` — обновление данных.
- `validate(): boolean` — (для форм) проверка корректности данных.
- `submit(): void` — (для форм) отправка данных.

Классы не создают друг друга напрямую, взаимодействие происходит через события и передачу экземпляров.  
Типы props и событий описаны в [`src/types/index.ts`](src/types/index.ts)

---

## 📁 Структура проекта

```
src/
  components/
    base/
      events.ts
      api.ts
  utils/
    utils.ts
    constants.ts
  common.blocks/    # SCSS стили
  pages/
    index.html
  index.ts          # точка входа приложения
  scss/
    styles.scss
```
